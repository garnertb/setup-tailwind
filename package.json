{
  "name": "setup-tailwind",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist",
    "compile": "tsc"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.7.3"
  }
}
